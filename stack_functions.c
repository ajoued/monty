#include "monty.h"

/**
 * push_ - pushes an element to the stack
 * @stack: stack to be treated
 * @line_number: number of the opcode line
*/
void push_(stack_t **stack, unsigned int line_number)
{
	stack = stack;
	line_number = line_number;
}
